import 'package:flutter/material.dart';

class FolderPageView extends StatelessWidget {
  const FolderPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("FolderPageView"),
    );
  }
}
